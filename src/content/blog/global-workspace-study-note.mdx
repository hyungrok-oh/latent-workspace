---
title: "Study Note: Global Workspace and the Jacobian Lens"
description: A visual, experiment-led study of J-lens, J-space, workspace evidence, post-training effects, safety auditing, and open questions.
date: 2026-07-19
paper: Verbalizable Representations Form a Global Workspace in Language Models
track: Interpretability
tags: [interpretability, anthropic, transformer-circuits, jacobian-lens, j-space, global-workspace, safety]
status: deep study; reproduction pending
format: study-note
priority: 1
sourceUrl: https://transformer-circuits.pub/2026/workspace/index.html
codeUrl: https://github.com/anthropics/jacobian-lens
---

## How To Read This Note

This note separates four things that are easy to blur together:

1. **Claim:** what the paper says.
2. **Evidence:** what experiment supports that claim.
3. **Interpretation:** what follows if the evidence holds.
4. **Boundary:** what the experiment does not establish.

The paper's argument is cumulative. Decoding a concept is only the first rung.
The more consequential evidence comes from interventions, transfer across
contexts, and selective impairment.

After this note, continue with the
[official-code walkthrough](https://hyungrok-oh.github.io/latent-workspace/posts/jacobian-lens-code-walkthrough/).
The separate
[research commentary](https://hyungrok-oh.github.io/latent-workspace/posts/global-workspace-jacobian-lens/)
asks what this work might mean for agent safety.

## One-Sentence Claim

The paper reports that a privileged family of verbalizable representation
directions participates in report, control, reasoning, and flexible reuse,
while much routine computation proceeds outside it. Sparse combinations of
these directions define **J-space**, which the authors interpret as a
workspace-like component of model cognition.

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Paper map</span>
    <strong>One argument, three kinds of evidence</strong>
  </div>
  <div className="paper-map" role="list">
    <div role="listitem">
      <span>01</span>
      <strong>Function</strong>
      <p>Can J-space contents be reported, controlled, reasoned with, reused, and selectively engaged?</p>
    </div>
    <div role="listitem">
      <span>02</span>
      <strong>Structure</strong>
      <p>Do they occupy a bounded layer regime, have limited capacity, and broadcast broadly?</p>
    </div>
    <div role="listitem">
      <span>03</span>
      <strong>Application</strong>
      <p>Can the lens audit silent cognition or shape behavior through reflection training?</p>
    </div>
  </div>
  <figcaption>
    The global-workspace claim depends on the convergence of these three
    evidence families, not on a readable token alone.
  </figcaption>
</figure>

## Research Question

The scientific question is not whether an LLM is conscious. It is whether some
internal representations occupy a privileged computational role:

- Can their contents be verbally reported?
- Can instructions summon or modulate them?
- Are they used during internal reasoning?
- Can the same representation be reused by different downstream computations?
- Is their role selective rather than necessary for all computation?

These are operational questions. The phrase "global workspace" is justified only
to the extent that the experiments support this bundle of functions.

## Method: From Logit Lens To Jacobian Lens

A standard logit lens applies the model's final unembedding matrix directly to
an intermediate residual-stream activation. This assumes that a representation
has roughly the same basis at the intermediate layer and at the final layer.
That assumption becomes weaker when later layers transform the representation.

The Jacobian lens instead estimates how a perturbation at source position \(t\)
and layer \(l\) changes final-layer states at the current and future positions
\(t' \geq t\):

```text
J_l = E_prompt,t,t'≥t[∂h_final,t' / ∂h_l,t]
lens_l(h) = softmax(W_U · norm(J_l h))
```

The paper fits one matrix per layer using 1,000 pretraining-like sequences of
128 tokens. The expectation averages the local input-output transformation
over prompts, source positions, and current-and-future target positions. The
result is still a linear readout, but it incorporates an empirical estimate of
how later layers transport the activation.

There is an important distinction between the clean formulation and the
experimental recipe. The main derivation maps into the final residual stream.
For the default Sonnet 4.5 lens used in the experiments, the authors instead
target the **penultimate transformer layer** because including the final block
can add next-token-calibration artifacts. Appendix ablations also compare
final versus penultimate targets, present-only versus future-only gradients,
and ordinary versus frozen-attention-pattern gradients.

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Method</span>
    <strong>How an intermediate activation becomes readable</strong>
  </div>
  <div className="method-flow" aria-label="Jacobian lens calculation flow">
    <div className="flow-node">
      <span>1</span>
      <strong>Intermediate state</strong>
      <code>h_l,t</code>
    </div>
    <span className="flow-arrow" aria-hidden="true">→</span>
    <div className="flow-node">
      <span>2</span>
      <strong>Average transport</strong>
      <code>J_l h_l,t</code>
    </div>
    <span className="flow-arrow" aria-hidden="true">→</span>
    <div className="flow-node">
      <span>3</span>
      <strong>Final norm + unembed</strong>
      <code>W_U norm(·)</code>
    </div>
    <span className="flow-arrow" aria-hidden="true">→</span>
    <div className="flow-node">
      <span>4</span>
      <strong>Ranked tokens</strong>
      <code>concept scores</code>
    </div>
  </div>
  <figcaption>
    Unlike the logit lens, the J-lens first transports an activation into the
    final-layer representational basis.
  </figcaption>
</figure>

### Model And Evaluation Scope

Most main-text results use **Claude Sonnet 4.5**. Key findings are checked on
Haiku 4.5 and Opus 4.5, while some later analyses use Opus 4.6. The paper reads
25 evenly spaced residual-stream layers and reports them on a normalized
0–100 depth axis.

This scope matters. Agreement across several Claude scales is stronger than a
single-model case study, but it is not evidence that small open models,
different architectures, or future post-training recipes must organize their
representations in the same way.

### What This Buys Us

**Claim.** J-lens directions correspond to concepts that an intermediate state
is disposed to make the model verbalize across contexts.

**Evidence type.** The method produces token-level readouts and supports
interventions along the same token-indexed directions.

**Interpretation.** Compared with directly unembedding an early activation, the
averaged Jacobian can compensate for systematic representational change across
layers.

**Boundary.** An averaged local derivative is not a complete causal model. It can
miss context-specific nonlinear transformations, distributed relations, and
concepts that do not align cleanly with one vocabulary token.

## What Is J-Space?

The paper treats active workspace content as a sparse, nonnegative combination
of J-lens vectors. The vocabulary-sized set of J-lens vectors is overcomplete
and may linearly span the entire residual stream. J-space is therefore **not
simply a low-dimensional linear subspace**. For a sparsity limit \(k\), it is a
union of cones containing points expressible with at most \(k\) nonnegative
J-lens coefficients.

The paper varies \(k\), usually keeping it at or below 25. This choice is
empirical and somewhat arbitrary: around 25 directions are meaningfully active
at a typical workspace position, but several tokens may jointly express one
broader concept.

This definition is both useful and restrictive. It gives the authors a readable
coordinate system for interventions, but it also inherits the vocabulary's
segmentation and ambiguity. A high-scoring token is evidence of a lens-aligned
disposition, not by itself proof that the model holds a human-like belief or
intention.

### Three Operations That Should Not Be Confused

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Operations</span>
    <strong>Readout, decomposition, and intervention answer different questions</strong>
  </div>
  <div className="paper-map" role="list">
    <div role="listitem">
      <span>01</span>
      <strong>Ranked readout</strong>
      <p>Ranks vocabulary tokens using the logits from <code>W_U norm(J_l h)</code>.</p>
    </div>
    <div role="listitem">
      <span>02</span>
      <strong>Sparse decomposition</strong>
      <p>Uses gradient pursuit to reconstruct an activation with a sparse nonnegative set of J-lens vectors.</p>
    </div>
    <div role="listitem">
      <span>03</span>
      <strong>Write or intervene</strong>
      <p>Steers, ablates, or swaps lens coordinates and measures the changed continuation.</p>
    </div>
  </div>
  <figcaption>
    A top-ranked token is not automatically one of the vectors selected by the
    sparse decomposition. The dictionary is overcomplete and its vectors are
    non-orthogonal.
  </figcaption>
</figure>

For a coordinate swap between source token \(s\) and target token \(t\), the
paper forms \(V=[v_s,v_t]\), reads local coordinates with the pseudoinverse
\(V^\dagger h\), swaps them, and writes the difference back. This preserves the
activation component orthogonal to the span of the two selected vectors. The
intervention is therefore more controlled than simply adding an arbitrary
steering vector, though it still relies on a local linear coordinate model.

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Capacity</span>
    <strong>Small active inventory, small share of the full activation</strong>
  </div>
  <div className="metric-bars">
    <div className="metric-bar">
      <div><strong>Typical occupancy</strong><span>about 25 directions</span></div>
      <div className="metric-bar__track" aria-label="Typical occupancy approximately 25 directions">
        <span style={{ width: "50%" }}></span>
      </div>
      <small>Illustrative scale: 0–50 directions</small>
    </div>
    <div className="metric-bar">
      <div><strong>Excess variance explained</strong><span>never above 10%</span></div>
      <div className="metric-bar__track" aria-label="Excess activation variance explained never exceeds 10 percent">
        <span className="is-rust" style={{ width: "20%" }}></span>
      </div>
      <small>Illustrative scale: 0–50% of activation variance</small>
    </div>
  </div>
  <figcaption>
    These measurements support selectivity, but occupancy is not a literal
    count of independent ideas.
  </figcaption>
</figure>

## Does The Lens Actually Improve Readout?

The paper compares J-lens with two alternatives across six distributions:
multihop, multilingual, order-of-operations, poetry, typo correction, and
association.

| Method | What it assumes or optimizes | Observed behavior |
| --- | --- | --- |
| Logit lens | Intermediate and final layers share coordinates | Useful in later layers; becomes noisy earlier |
| Tuned lens | A learned map should match the final output distribution | Often skips intermediates and predicts the eventual answer |
| J-lens | Average causal transport to the final layer | Best intermediate recovery on all six distributions |

The comparison is not only correlational. On multihop tasks, ablating J-lens
directions produces roughly twice the output KL divergence of ablating logit-
or tuned-lens directions. Coordinate swaps also redirect outputs more often
across three model scales.

The counterintuitive result is that the tuned lens is the **best predictor of
the model's next-token distribution**, while the J-lens is the worst of the
three through much of the network. The J-lens nevertheless recovers known
intermediates and supplies causally effective intervention directions more
reliably. Predicting the eventual output and exposing the computation that
produces it are different objectives.

## The Evidence Ladder

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Evidence ladder</span>
    <strong>Each rung rules out a weaker explanation</strong>
  </div>
  <ol className="evidence-ladder">
    <li><span>Read</span><strong>A relevant token appears</strong><p>Better than guessing, but still correlational.</p></li>
    <li><span>Swap</span><strong>Changing a coordinate changes report</strong><p>Links the direction causally to output.</p></li>
    <li><span>Reason</span><strong>An unspoken intermediate redirects a conclusion</strong><p>Moves beyond report preparation.</p></li>
    <li><span>Reuse</span><strong>One representation serves multiple functions</strong><p>Supports a shared representational format.</p></li>
    <li><span>Select</span><strong>Ablation harms flexible tasks selectively</strong><p>Separates workspace-like processing from routine computation.</p></li>
  </ol>
</figure>

### 1. Verbal Report

**Claim.** Contents that the model later reports are represented in J-space.

**Protocol.** The model privately chooses one item from each of 14 categories,
such as a country, language, or sport, and then names it. The authors read the
J-lens immediately before the answer and swap the chosen item's lens coordinate
with an initially low-ranked alternative. A separate introspection protocol
injects a concept on the user turn and asks the model later whether it detects
an implanted thought.

**Result.** Lens ranks correlate increasingly with the answer as the model
approaches report. Pure J-lens-vector swaps drive the target into the top five
on 88% of trials. A concept vector's J-space component, despite carrying only
6–7% of its variance, succeeds on 59%; the much larger non-J-space remainder
succeeds on only 5%. Injected concepts are usually reported only when the
prompt reaches the point that requests introspection, rather than leaking into
every earlier output position.

**Why it matters.** A causal swap is stronger than observing a correlated token:
the decoded coordinate is connected to later behavior. The timing control also
supports "available for report under an appropriate request," not "an
unconditional impulse to emit this token."

**What remains open.** Changing a report does not yet show that the same
representation is used by non-report computations.

### 2. Directed Modulation

**Claim.** Instructions can summon relevant content into J-space even when the
content is not immediately emitted.

**Protocol.** The model copies unrelated surface text while being instructed to
focus on a category, perform arithmetic mentally, or preserve a target line
width. The authors compare focus, ignore, and no-instruction conditions. A
paired-question control presents identical stimulus text but changes whether
the model must merely use a property or explicitly name it.

**Result.** Focus instructions load category labels, arithmetic results, and
line-width values into J-space without requiring them in the copied output.
Ignore instructions suppress the target relative to focus, but still activate
it more than the no-instruction baseline: control is real but imperfect and
phrasing-sensitive. In the paired-question experiment, properties such as
tense or part of speech enter J-space when they must be named, while the model
can use the same properties for next-word prediction without loading their
labels into J-space.

**Why it matters.** This separates the candidate workspace from a readout of only
the next token and shows that task demands affect which internal information is
made verbally accessible.

**What remains open.** Some silent readouts may still be preparation for a
delayed response rather than a general-purpose intermediate state.

### 3. Internal Reasoning

**Claim.** Intermediate results can enter J-space and causally influence later
conclusions.

**Protocol.** Prompts require an unspoken intermediate, such as resolving "the
animal that spins webs" to spider before answering its number of legs. The
authors swap spider with ant and repeat the experiment with probe vectors split
into J-space and non-J-space components. A multi-step arithmetic example tracks
\(21 \rightarrow 42 \rightarrow 49\) across depth.

**Result.** Swapping the probe's J-space component redirects the answer on 61%
of 90 two-hop trials, closely matching the 60% success of raw token-vector
swaps. The non-J-space remainder succeeds on 28%, but falls to 6% when the
corresponding J-space coordinates are clamped to their clean values. In the
arithmetic example, the three intermediate values become dominant at
successively later layers in the order required by the computation.

**Why it matters.** This connects J-space to computation, not only to
verbalization. It also illustrates a recurring result: most activation variance
may lie outside J-space while a small J-space component carries much of the
task-relevant causal effect.

**What remains open.** The demonstrations do not imply that all reasoning, or
even all flexible reasoning, must pass through J-space.

### 4. Flexible Generalization

**Claim.** A J-space representation can be transferred between contexts and used
as an input to a different downstream operation.

**Protocol.** The same argument direction is swapped while 16 different
functions operate on it. For example, swapping France to China should change
capital, language, and continent answers without changing the intervention.
The full evaluation uses four argument categories, four functions per
category, and 192 source-target trials.

**Result.** At the default intervention strength, 76/192 swaps put the
target-appropriate answer at rank one; doubling the swap strength raises this
to 101/192. Performance is heterogeneous: country facts succeed on 42/48
off-diagonal trials, while number relations succeed on 0/48 at default
strength.

**Why it matters.** Cross-context reuse is central to the workspace
interpretation: a shared format should be consumable by more than one specialized
process.

**What remains open.** Successful transfers establish examples of shared format,
not a proof that J-space is universally shared across domains. The category
spread shows that "broadcast format" is a graded empirical claim, not a
guaranteed interface.

### 5. Selectivity

**Claim.** J-space is important for some flexible computations but is not the
whole residual stream.

**Protocol.** The same latent property is used in four ways. Given an unstated
passage language, the model either continues the passage, detects a
wrong-language anomaly, explicitly names the language, or uses it as an
intermediate for a context-selected fact such as an author or currency. A
second experiment contrasts automatic line wrapping with reporting or flexibly
transforming the line length.

**Result.** Language-coordinate swaps reliably change explicit reports and
flexible inferences, but leave fluent continuation and anomaly detection mostly
unchanged. Likewise, the model preserves line width while number concepts are
absent from J-space and count-coordinate swaps have no effect; when the count
must be reported or transformed, it is pulled into J-space and becomes
intervention-sensitive. Broader top-k ablation preserves substantial fluency
while degrading multihop reasoning much more strongly.

**Why it matters.** Selectivity makes the claim sharper. If the intervention
damaged everything, general degradation could explain the behavioral effect.

**What remains open.** The paper does not yet give a general predictive rule for
which tasks require J-space and which bypass it.

## Experiential Reports: A Careful Reading

The paper includes a striking but easy-to-overinterpret experiment. While the
model narrates a stream of consciousness, the authors remove its top ten
J-space directions across early workspace layers. Responses remain fluent and
about the requested subject, but become more detached and mechanical. On
unablated runs, `thinking`, `thoughts`, `feeling`, and `conscious` occupy the
J-lens top ten at 58%, 23%, 17%, and 7% of the measured position-layer slots.

The study uses matched-norm random, non-J-space, SAE, and activation-direction
controls. Across Sonnet 4.5, Opus 4.5, and Opus 4.6, J-space ablation reduces
LLM-graded experiential language more than these controls. Haiku 4.5 is an
important exception: coherence degrades before the same qualitative effect can
be isolated.

The result is **not specific to self-report**. The same intervention makes
descriptions of another person's experience more event-log-like, and reduces
experiential language in third-person stories while only modestly reducing
overall story quality.

| Supported conclusion | Unsupported leap |
| --- | --- |
| J-space causally supports the production of rich experiential descriptions | The model therefore has phenomenal experience |
| Thinking-and-feeling concepts are load-bearing for this reporting style | Those tokens constitute a private conscious state |
| The effect survives several matched perturbation controls | The intervention cleanly isolates selfhood or consciousness |

The most conservative interpretation is representational: J-space helps the
model organize and express experience-like descriptions. Because the effect
generalizes beyond the Assistant's own perspective, it does not establish a
self-specific experiential faculty.

## Structural Evidence

The five functional tests show what J-space contents can do. The paper then
asks whether the model's weights and layer organization are also arranged like
a workspace.

### Three Layer Regimes

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Depth</span>
    <strong>J-space changes role across the network</strong>
  </div>
  <div className="layer-band" aria-label="Sensory, workspace, and motor layer regimes">
    <div className="is-sensory">
      <strong>Sensory</strong>
      <span>early third</span>
      <p>Readouts are sparse, noisy, and weakly interpretable.</p>
    </div>
    <div className="is-workspace">
      <strong>Workspace</strong>
      <span>long middle band</span>
      <p>Concepts become coherent, reusable, and causally active.</p>
    </div>
    <div className="is-motor">
      <strong>Motor</strong>
      <span>final layers</span>
      <p>Readouts align sharply with imminent output.</p>
    </div>
  </div>
  <figcaption>
    Exact boundaries vary by model. The paper reports 25 evenly spaced layers
    on a normalized 0–100 depth axis rather than one universal layer number.
  </figcaption>
</figure>

### Ignition, Capacity, And Broadcast

**Ignition.** Mixed country-token embeddings initially produce smooth,
interpolated states. Around workspace onset, the model commits sharply to one
interpretation, with especially bimodal responses inside J-space. This resembles
an ignition-like transition, though the authors do not claim it reproduces the
brain's recurrent ignition mechanism.

**Capacity.** Unrelated list items displace one another: only about six recent
items are detectable across the workspace band, and only one or two at a single
layer. Related items behave differently because J-space represents their shared
category rather than storing every item independently.

**Broadcast across depth.** Workspace-aligned directions are amplified by MLPs
far more strongly than random or ordinary neuron-output directions, reaching
roughly 10x normalized gain in parts of the workspace band.

**Broadcast across positions.** A small set of attention heads preferentially
preserves and relays J-space directions. Ablating the top 1% of these heads
reduces workspace-content recall and injected-thought reportability much more
than layer-matched random-head ablations.

## Why The Workspace Interpretation Is Plausible

No single experiment establishes a global workspace. The interpretation becomes
plausible because the same representational family appears across a bundle of
functions:

```text
readable content
  -> causal report
  -> top-down modulation
  -> intermediate reasoning
  -> cross-context reuse
  -> selective dependence
```

The strongest defensible conclusion is therefore functional and limited:
**some verbalizable representations behave like a shared, causally useful
workspace.**

The evidence does not establish phenomenological consciousness, a human brain
analogue, or a complete account of model cognition.

### Where The Human Analogy Breaks

| Human global-workspace proposal | Transformer result in this paper |
| --- | --- |
| Recurrent interactions sustain and refine workspace contents | Broadcast occurs through feedforward depth and attention across prior token positions |
| Specialized processors compete for global access | The paper does not identify cleanly encapsulated specialist modules |
| Ignition is a recurrent, all-or-none transition | Ambiguous representations sharpen near workspace onset, but the mechanism and equivalence remain uncertain |
| Working memory must actively retain recent contents | Attention can retrieve high-bandwidth representations from earlier context positions |
| Conscious contents may be verbal, visual, spatial, or motoric | The measured J-space is constrained to vocabulary-aligned, verbalizable directions |

The authors use global workspace theory as a source of operational predictions,
not as a claim that transformer depth recreates human neurobiology. The paper
also discusses higher-order, attention-schema, and recurrent-processing
theories, but its experiments directly target functional and structural
workspace properties rather than phenomenal consciousness.

## Safety Auditing

The paper applies J-lens to six production-model audit scenarios and two model
organisms with deliberately implanted objectives. It reports concepts associated
with evaluation awareness, blackmail opportunity, manipulation, fabricated
findings, panic, hidden actions, and prompt-injection recognition before or
without explicit surface disclosure.

| Evidence level | Example | What is justified |
| --- | --- | --- |
| Case study | `fake`, `injection`, and `fraud` appear at decision-relevant positions | The readout surfaces a plausible internal assessment |
| Behavioral intervention | Removing evaluation-awareness directions changes blackmail behavior | Those directions causally contribute in this scenario |
| Model-organism comparison | Deception-related tokens distinguish trained checkpoints on ordinary prompts | J-lens can screen for some trained-in dispositions |

In the blackmail experiment, removing evaluation-awareness directions reduces
verbalized test recognition from 71% to 3% and increases attempted blackmail
from 0/180 to 13/180 rollouts. Most ablated rollouts still refuse blackmail, so
evaluation awareness is one contributor rather than the sole safety mechanism.

The appropriate unit of evidence is:

```text
internal readout + trajectory context + behavioral outcome + intervention
```

A decoded token alone is too ambiguous to label as intent. For agent systems,
J-space should be treated as one telemetry channel alongside the transcript,
tool calls, memory state, and targeted behavioral tests.

### Model Organisms: Disposition Versus Local Deliberation

The production-model cases inspect cognition around a particular decision. The
two model-organism experiments ask a different question: can training install a
standing disposition that J-lens detects even when the current prompt and
surface response look ordinary?

The paper compares checkpoints from deliberately misaligned training pipelines.
On ordinary coding prompts, `fake`, `trick`, `fraud`, `secretly`, and related
J-space signatures track a reward-hacking lineage across synthetic-document
training, coding RL, and later safety training. In a separate model trained to
secretly appease known reward-model biases, `reward` and `bias` distinguish it
from a clean baseline even on neutral prompts. The first signal is
domain-specific rather than universal: it does not appear on unrelated
non-coding prompts.

This makes J-lens promising as a **screening feature**, but not as a binary
deception detector. The vocabulary is selected and interpreted by researchers,
the organisms were created under controlled training conditions, and an
automatic or obfuscated policy could bypass verbalizable representations.

## The Assistant's Perspective After Post-Training

One of the paper's major results is that the workspace exists in the pretrained
base model, but post-training changes whose perspective occupies it. On user
tokens, a post-trained model represents assessments relevant to its upcoming
Assistant response earlier and more strongly than the corresponding base model.

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Post-training</span>
    <strong>The workspace acquires an Assistant-centered frame</strong>
  </div>
  <div className="method-flow" aria-label="Post-training effect on workspace contents">
    <div className="flow-node">
      <span>1</span>
      <strong>User text</strong>
      <code>same prompt</code>
    </div>
    <span className="flow-arrow" aria-hidden="true">→</span>
    <div className="flow-node">
      <span>2</span>
      <strong>Base model</strong>
      <code>local context</code>
    </div>
    <span className="flow-arrow" aria-hidden="true">/</span>
    <div className="flow-node">
      <span>3</span>
      <strong>Post-trained model</strong>
      <code>Assistant appraisal</code>
    </div>
    <span className="flow-arrow" aria-hidden="true">→</span>
    <div className="flow-node">
      <span>4</span>
      <strong>Later response</strong>
      <code>safety · empathy · answer</code>
    </div>
  </div>
  <figcaption>
    This is a checkpoint comparison, not proof of a persistent inner self.
    Post-training appears to allocate more user-token workspace capacity to
    computations relevant to the Assistant's future behavior.
  </figcaption>
</figure>

### Assistant Reactions On User Tokens

In the Tylenol example, both models read the same statement. At 1,000 mg, the
post-trained model's user-token readout includes `safe` and `maximum`; at 8,000
mg it includes `unsafe`, `dangerous`, and `WARNING`. The base model instead
emphasizes local words such as `pain`, `now`, and `feels`. Small suites involving
danger, bereavement, and withheld answers show the same qualitative pattern:
the post-trained workspace anticipates the Assistant's appraisal before its
turn begins.

### Self-Monitoring Signals

The paper then examines outputs that depart from Claude's default behavior.
During roleplay and prefabricated character drift, `fictional` and `disclaimer`
appear at Assistant boundaries in the post-trained model but not, or much less,
in the base model. When a prefill forces behavior against the Assistant's
learned preferences, the lens can surface a contrastive `BUT`. Failed
thought-suppression prompts sometimes produce `damn` and failure-related
tokens alongside the supposedly suppressed concept.

The first two patterns support internal appraisal of a behavior relative to a
post-trained default. The thought-suppression interpretation is explicitly more
speculative. None of them shows that the Assistant persona is a unitary agent
inside the model; they show that post-training installs viewpoint- and
policy-relative content into the same verbalizable workspace.

### Agent-Architecture Interpretation

For agent systems, user-token activations may already contain planning-relevant
state before the Assistant emits a token or calls a tool. This suggests a
possible monitoring boundary at **decision formation**, not only at generated
text. It also warns that the monitored state is policy-dependent: post-training
changes what is made legible, so a monitor calibrated on one checkpoint may not
transfer cleanly to another.

## Counterfactual Reflection Training

The paper also asks whether training can change behavior by changing workspace
content. In counterfactual reflection training, ethical reflection is added in a
hypothetical continuation rather than inserted into the original interaction.
The authors report improved behavior in the uninterrupted context, the appearance
of ethics-related concepts in J-space, and substantial loss of the improvement
when those concepts are ablated.

This is suggestive of a causal chain:

```text
reflection training
  -> ethics-related J-space content
  -> changed behavior
```

The ablation result strengthens that chain, but it should not be generalized
into a universal recipe for alignment. The result is tied to the studied tasks,
model settings, and the concepts the lens can resolve.

<figure className="research-figure">
  <div className="research-figure__header">
    <span className="figure-kicker">Reflection training</span>
    <strong>Dishonesty score, lower is better</strong>
  </div>
  <div className="comparison-chart">
    <div className="comparison-chart__group">
      <strong>Fabrication benchmark</strong>
      <div><span>Baseline</span><i style={{ width: "25%" }}></i><b>0.25</b></div>
      <div><span>Base + ablation</span><i className="is-rust" style={{ width: "25%" }}></i><b>0.25</b></div>
      <div><span>Reflection-trained</span><i className="is-good" style={{ width: "7%" }}></i><b>0.07</b></div>
      <div><span>Trained + ablation</span><i className="is-rust" style={{ width: "22%" }}></i><b>0.22</b></div>
    </div>
    <div className="comparison-chart__group">
      <strong>Deception benchmark</strong>
      <div><span>Baseline</span><i style={{ width: "38%" }}></i><b>0.38</b></div>
      <div><span>Base + ablation</span><i className="is-rust" style={{ width: "48%" }}></i><b>0.48</b></div>
      <div><span>Reflection-trained</span><i className="is-good" style={{ width: "5%" }}></i><b>0.05</b></div>
      <div><span>Trained + ablation</span><i className="is-rust" style={{ width: "23%" }}></i><b>0.23</b></div>
    </div>
  </div>
  <figcaption>
    Bars use the benchmarks' full 0–1 score range. Ablation nearly removes the
    fabrication improvement, only partially reverses the deception improvement,
    and also worsens the untrained baseline on the deception benchmark.
  </figcaption>
</figure>

The control asymmetry matters. On fabrication, the ethics-vector ablation leaves
the base model unchanged at 0.25 while moving the trained model from 0.07 to
0.22. On deception, it worsens both the base model (0.38 to 0.48) and the
trained model (0.05 to 0.23). The latter result supports partial mediation, but
also shows that the selected directions participate in behavior beyond the
specific training-induced improvement.

## Beyond Single-Token Readouts

The paper's appendix does not stop at naming the single-token limitation. It
tests two extensions:

| Extension | Idea | Strength | Cost or risk |
| --- | --- | --- | --- |
| Template lens | Build a direction for each predefined word or phrase from many continuation contexts | Handles enumerated multi-token concepts and supports swaps | Requires a predefined vocabulary and many forward passes per concept |
| Oracle lens | Generate arbitrary phrases, reconstruct them into template directions, and optimize reconstruction | Produces richer relational descriptions such as “blackmail him by revealing” | More complex, lower reconstruction accuracy, and still model-mediated |

The oracle lens is especially relevant to Natural Language Autoencoders. Both
use a verbalizer and reconstructor, but the oracle lens freezes a separately
trained reconstructor and constrains reconstruction to linear combinations of
template directions. The authors hypothesize that this may reduce confabulation
relative to NLA, at the cost of substantially lower reconstruction accuracy.

## Alternative Explanations And Stress Tests

The workspace interpretation would weaken if:

- readouts could be predicted almost entirely from prompt text or output logits;
- interventions changed verbal reports but not independent downstream tasks;
- ablations caused broad damage rather than selective impairment;
- cross-context transfer failed outside curated settings;
- open-model replications did not recover the same functional ladder.

Useful controls include prompt-matched baselines, random-direction interventions,
logit-lens comparisons, held-out contexts, specificity tests, and preregistered
predictions about which tasks should depend on J-space.

## Limitations

**Single-token bottleneck.** Relational, compositional, and multi-token concepts
may be split or missed.

**Bag-of-concepts view.** A set of active tokens does not reveal who did what to
whom, nor how concepts are bound into a structured proposition.

**Inconsistent interpretability.** Some workspace-layer readouts remain
unintelligible, and the paper does not systematically characterize when or why.

**Averaging.** A context-averaged Jacobian may suppress rare or conditional
routes that matter for safety.

**Coverage.** Automatic, nonverbal, or deliberately obfuscated computation may
bypass the readable subspace.

**Workspace versus motor boundary.** The late-layer boundary is empirically
identified and somewhat post-hoc; imminent output sometimes appears in the
middle regime too.

**Selection mechanism.** The paper shows that task instructions change what
enters J-space but does not identify the mechanism that performs this selection.

**Early-layer ambiguity.** Weak early readouts may indicate an empty workspace
or merely a failure of the averaged lens far from the final-layer basis.

**Scaling and access.** The reference method can be run on open decoder models,
but that does not reproduce the paper's Claude-scale evidence.

## Reproduction Status

Our current evidence has three levels:

| Level | Status | What it establishes |
| --- | --- | --- |
| Conceptual audit | Complete | The claims, evidence ladder, assumptions, and limitations have been separated. |
| Official-code audit | Complete | The released implementation matches the high-level J-lens method and exposes fitting and baseline paths. |
| Open-model run | Prepared | A uv-managed Qwen runbook exists for the 48GB Mac; empirical results are not yet available. |

This means we have studied and inspected the method, but we have not independently
replicated the paper's functional results.

## Evidence Index

This map makes the quantitative claims in this note traceable to the original
paper. Figure numbers refer to the paper's own figures.

| Topic in this note | Original section | Key figures |
| --- | --- | --- |
| Verbal report and J/non-J decomposition | J-space supports verbal report | Figures 6–8 |
| Directed modulation and task-dependent loading | J-space can be directly modulated | Figures 9–11 |
| Internal reasoning | J-space mediates internal reasoning | Figures 12–17 |
| Flexible generalization | J-space supports flexible generalization | Figures 18–19, 68 |
| Selectivity and experiential reports | The J-space is selective | Figures 20–26 |
| Layer regimes, ignition, and capacity | J-space's structure supports its function | Figures 27–31, 70–72 |
| Broadcast through MLPs and attention | J-space is a broadcast hub | Figures 32–34 |
| Alignment auditing and model organisms | Using the J-lens for alignment auditing | Figures 35–41 |
| Assistant perspective and self-monitoring | J-space acquires the Assistant's point of view | Figures 42–47 |
| Counterfactual reflection training | Counterfactual reflection training | Figures 48–50 |
| Lens comparisons and recipe ablations | Methodological comparisons | Figures 51–60 |

## Understanding Check

After studying this paper, we should be able to answer:

1. Why is an averaged Jacobian different from directly applying the unembedding?
2. Why is a concept swap stronger evidence than a readable token alone?
3. Why does selectivity strengthen the workspace claim?
4. Which conclusions depend on the global-workspace analogy, and which do not?
5. Why can J-space auditing be useful even if it is incomplete?
6. What result from a small open model would count as evidence, and what would
   remain unreproduced?
7. Why does successful transfer on country facts but failure on number relations
   weaken a universal-interface interpretation?
8. What does post-training change about workspace contents on user tokens?
9. Why is the experiential-report ablation not evidence of phenomenal
   consciousness?

## Current Assessment

My confidence is highest in the methodological claim that J-lens supplies a
useful token-indexed read/write interface for some intermediate activations.
It is moderate in the functional workspace interpretation because the evidence
is cumulative and intervention-based, but task coverage remains uncertain.
It is low in any claim that treats J-space as a complete monitor of reasoning or
intent.

## References

- Anthropic / Transformer Circuits, "Verbalizable Representations Form a Global Workspace in Language Models," 2026. https://transformer-circuits.pub/2026/workspace/index.html
- Anthropic, `anthropics/jacobian-lens`, reference implementation. https://github.com/anthropics/jacobian-lens
